# AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked in Linear, accessed via the Linear MCP server. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default canonical strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
